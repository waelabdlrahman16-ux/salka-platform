// ⚠️ PLACEHOLDER — replace with the real InstaPay handle/phone number before
// going live. This is what customers see and copy/send money to at checkout.
export const INSTAPAY_HANDLE = 'REPLACE_ME@instapay'

// Optional: if you have a QR code image for your InstaPay handle, drop it in
// /public (e.g. /instapay-qr.png) and set the path here. Leave null to hide
// the QR and show only the handle + copy button.
export const INSTAPAY_QR_URL: string | null = null
