{
  "project_ref": "pqpnwxyevrsipklzmwex",
  "schemas": ["public", "private"],
  "captured_at": "2026-08-11T14:45:00+00:00",
  "previous_capture": "2026-08-10T10:30:41+00:00",
  "postgres_version": "17.6",
  "snapshot_only": true,
  "includes_data_rows": false,
  "counts": {
    "tables": 48,
    "views": 0,
    "functions_public": 163,
    "functions_private": 99,
    "functions_total": 262,
    "functions_security_definer": 243,
    "policies": 98,
    "triggers": 14,
    "indexes": 119,
    "migrations": 324
  },
  "drift_since_previous_capture": {
    "reason": "No schema-hardening batch this round -- three targeted changes since the 2026-08-10 capture, verified individually against the live catalog before this refresh (not assumed from migration names alone).",
    "migrations": "+3 (321 to 324): consolidate_multiple_permissive_rls_policies, allow_vendor_manage_own_delivery_slots (matches PR #82, already shipped), notify_customer_on_custom_order_price_confirmed (this capture's change)",
    "policies": "+19 (79 to 98) from consolidate_multiple_permissive_rls_policies -- multiple role-specific PERMISSIVE policies per table/action (e.g. separate 'admin writes X' / 'catalog writes X' / 'supervisor reads X') were consolidated into fewer, broader per-verb policies (e.g. 'manage X insert/update/delete' + one read policy). Verified this is a consolidation, not a loosening: every consolidated policy's qual/with_check still gates on the same protected predicates (is_admin(), is_catalog_manager(), my_driver_id(), my_restaurant_id(), is_supervisor(), supervisor_may_touch_order()) that gated the original split policies. delivery_slots gained restaurant_id = my_restaurant_id() as an alternative to is_admin() on insert/update, matching allow_vendor_manage_own_delivery_slots (PR #82: vendors can now manage their own restaurant's delivery slots).",
    "functions_public": "unchanged (163)",
    "functions_private": "unchanged (99)",
    "functions_security_definer": "unchanged (243)",
    "tables": "unchanged (48)",
    "triggers": "unchanged (14) -- only a function body changed (notify_order_status_change gained a pricing_status='confirmed' branch so customers get a push when a supervisor prices their custom order), not the trigger definition itself",
    "indexes": "unchanged (119)"
  },
  "verified_at_capture": {
    "policyless_tables_readable_by_anon_or_authenticated": 0,
    "residual_anon_or_authenticated_select_on_the_7_tables": 0,
    "rls_predicates_holding_authenticated_execute": 7,
    "service_role_only_wrappers_leaking": 0,
    "anon_executable_security_definer_functions_in_public": 0,
    "authenticated_executable_security_definer_functions_in_public": 7,
    "advisor_warnings_total": 20,
    "advisor_floor_note": "12 rls_enabled_no_policy (INFO, intentional) + 1 extension_in_public (pg_net, not remediated per ACCEPTED-WARNINGS.md) + exactly the 7 protected RLS predicates. Re-verified via get_advisors at this capture -- unchanged from the 2026-08-10 floor."
  },
  "notes": [
    "routines.json carries schema, per-role execute grants and full definitions for BOTH public and private. This capture only updated the one entry that actually changed (public.notify_order_status_change) -- every other routine definition was left as captured 2026-08-10 and is not independently re-verified here.",
    "policies.json was fully regenerated from a live pg_policies query at this capture (all 98 rows), not incrementally patched, since the consolidation migration touched roughly a quarter of all policies.",
    "grants.json, triggers.json, and indexes.json were NOT regenerated this capture -- their live counts were spot-checked and match the 2026-08-10 capture exactly (triggers 14, indexes 119), and no migration since then touched grants, so the existing files are still accurate. Full regeneration deferred to the next capture that actually changes one of them.",
    "database.types.ts was NOT regenerated this capture -- no public-schema table/column shape changed (only a policy consolidation and one trigger-function body), so the existing generated types are still accurate.",
    "No public routine was removed by name: every core relocated to private in batches 8-9 kept a same-named public wrapper.",
    "The `Supabase Preview` CI check fails on every merge to main with 'Remote migration versions not found in local migrations directory' -- see supabase/baseline/README.md. This capture's own migration file (20260811143000_...) has the same local-timestamp-vs-MCP-applied-version mismatch as every migration since 2026-08-09, for the same reason.",
    "This capture closes out the ACCEPTED-WARNINGS.md item that incorrectly claimed a PGRST202 fallback shim existed in 11 Edge Functions -- verified against the actual code (21 files, 60 .rpc() calls) that no such shim exists anywhere; the prior documentation was wrong, not the code. See docs/SECURITY-AUDIT-CLOSURE.md and ACCEPTED-WARNINGS.md for the correction."
  ]
}
